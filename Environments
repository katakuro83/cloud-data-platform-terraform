Environments/dev and environments/prod thin composition roots wiring the modules together with separate state files, separate CIDR ranges, and different warehouse sizing

Dev environment backend config

Dev environment main.tf composing all modules

Dev environment variables

Dev environment outputs

Dev environment example tfvars
